# NCS 세분류 추출·매칭 6시간 작업 기록

- 작업 창: 2026-08-26 23:36:16 KST ~ 2026-08-27 05:36:16 KST
- 범위: 문서의 명시적 세분류 추출, 현행 공식 세분류·능력단위 매칭, 검토 제안 경계
- 오케스트레이션: Luna(코드 경로·UI), Terra(좌표 추출·신뢰성), Sol(독립 매칭 리뷰), root(통합·검증)

## 결과

1. 좌표 표의 명시적 세분류 헤더와 공식 exact 값을 안전한 추출 후보로 복구한다.
2. 동일 공식 세분류로 정규화되는 표기 변형은 최종 후보에서 안정적으로 중복 제거한다.
3. ALIO 프로필 제안은 실제 추출 후보와 분리하고 자동 선택·능력단위 자동 귀속을 막는다.
4. MCP 행은 `path.sub` 공식명과 능력단위 코드의 세분류 8자리 prefix가 같은 카탈로그 항목을 가리킬 때만 수용한다.
5. 잘린 코드·비숫자 base·잘못된 version suffix를 거부하고, 적용된 세분류 해소 규칙을 metadata로 남긴다.
6. 활성 세분류명·코드, 안전 별칭, 능력단위 detail identity의 정적 불변식을 테스트로 고정한다.

## 검증

- 관련 통합 pytest: `399 passed` (Starlette 의존성 deprecation warning 1건)
- Ruff: 변경 Python 파일 전체 통과
- `git diff --check`: 통과
- 공개 ALIO 2문서 운영 MCP probe: parse `2/2`, 공식 세분류 exact `16/16`

공개 probe는 네트워크·회귀 확인용이며 사람 골드 정확도나 신규 블라인드 평가가 아니다. 기존에 관측된 validation/holdout 자료는 규칙 설계·선택·성능 주장에 사용하지 않았다.

## 후속 과제

- 능력단위는 정적 카탈로그의 `full code exact`와 `base + canonical name + verified detail`을 계층화해 검증하면 같은 세분류 내부의 stale 검색 행까지 더 강하게 차단할 수 있다.
- 새 사람 골드는 공정채용 사이트 신규 문서를 별도 보관하고, 원문 독립 검수·불일치 조정·물리적으로 분리된 reference로 한 번만 평가해야 한다.
